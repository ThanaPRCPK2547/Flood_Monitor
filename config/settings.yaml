data_source:
  mode: csv_mockup
  csv_path: data/thailand_flood_sample.csv

processing:
  min_samples_per_province: 300

storage:
  schema: ""
  table: flood_risk_events

paths:
  output_dir: data/output

# Data source (override if needed)
FLOOD_DATASET_PATH=/Users/thanakorn/Desktop/thailand_flood_mockup_1M.csv

# PostgreSQL / PostGIS
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/flood_db

# Optional runtime overrides
FLOOD_CONFIG_PATH=config/settings.yaml
FLOOD_SCHEMA=public
FLOOD_TABLE=flood_risk_events
START_DATE=
END_DATE=
